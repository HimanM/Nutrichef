# This is an example environment file for the backend service.
# Copy this file to .env in the project root (not in the backend directory)
# for docker-compose to pick it up automatically, or set these variables
# directly in your system environment or docker-compose.yml.

# --- Database Configuration ---
# These values should match those for the 'db' service in docker-compose.yml
DB_HOST=db
DB_PORT=3306
DB_NAME=myapp_db
DB_USER=myapp_user
DB_PASS=myapp_password_please_change

# --- Flask Specific ---
FLASK_APP=app.py
FLASK_RUN_HOST=0.0.0.0
# FLASK_ENV=development # Uncomment for development mode
# FLASK_SECRET_KEY=your_strong_flask_secret_key_here # Important: Set a strong secret key

# --- Mail Server Configuration ---
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password_or_actual_password
MAIL_DEFAULT_SENDER=your_email@gmail.com

# --- Gemini NLP Configuration ---
GEMINI_API_KEY=your_gemini_api_key_here
PROJECT_NUMBER=your_google_cloud_project_number_here

# --- Frontend URL ---
# Used by the backend if it needs to generate absolute URLs pointing to the frontend.
# This should match the port your frontend is accessible on.
FRONTEND_URL=http://localhost:3000

# --- Other ---
# PYTHONUNBUFFERED=1 # Set to a non-empty value to ensure print() statements appear without delay in Docker logs
